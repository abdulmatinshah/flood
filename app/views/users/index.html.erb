<% provide(:pae_title, 'All users') %>
<h1>All users</h1>
<div class="users">
  <ul>
    
      <% @users.each do |user|  %>
      <%= render 'shared/user', user: user %>
      <%#= render user %>
      <% end %>
  </ul>
  <%= will_paginate %>
</div>
<br>
<%= link_to 'New User', new_user_path %>
